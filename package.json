{
  "name": "directions",
  "version": "1.0.0",
  "description": "print directions for optimal route between a given list of addresses",
  "main": "index.js",
  "scripts": {
    "start": "npm run directions",
    "directions": "node index.js",
    "default": "echo '1 Décarie Street, Montreal, QC' >> adresses && npm run directions adresses",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "kareniel",
  "license": "MIT",
  "dependencies": {
    "nanohtml": "^1.2.4",
    "request": "^2.85.0",
    "request-promise": "^4.2.2"
  },
  "devDependencies": {
    "debug": "^3.1.0"
  }
}
